-- CreateTable
CREATE TABLE "Parametres" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "anneeDebut" INTEGER NOT NULL,
    "periodeActive" INTEGER NOT NULL,
    "anneesCreees" TEXT NOT NULL
);
